[failed_login]
search = index=linux_secure "Failed password"
description = SSH or terminal login failure events parsed from /var/log/secure.

[high_severity_alert]
search = severity=high OR severity=critical
