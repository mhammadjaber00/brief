[brief_default_index]
definition = index=main OR index=security
description = Default index set used across the security saved searches in this app.

[noise_filter(1)]
args = field
definition = NOT $field$ IN ("system", "noreply", "monitoring")
