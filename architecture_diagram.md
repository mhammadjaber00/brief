# Architecture diagram

Placeholder. Finalize in Step 09 (demo + submission). The hackathon rules require a file at repo root named `architecture_diagram.png`, `.md`, or `.pdf` — this file satisfies the requirement until the final diagram lands.
